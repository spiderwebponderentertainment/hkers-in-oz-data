name: Scrape SBS EN

on:
  schedule:
    - cron: "35 * * * *"   # 每小時第 35 分跑
  workflow_dispatch: {}

concurrency:
  group: scrape-sbs-en
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30  # ✅ 關鍵 1：防止跑到一半被殺死
    permissions:
      contents: write
    env:
      REPO_SLUG: spiderwebponderentertainment/hkers-in-oz-data
      FEED_JSON: sbs_en.json
      FEED_XML:  sbs_en.xml

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: Configure git
        run: |
          git config user.name "news-bot"
          git config user.email "news-bot@users.noreply.github.com"

      - name: Rebase onto latest main
        run: |
          git fetch origin main
          git rebase origin/main || (git rebase --abort && echo "Rebase conflict, aborting." && exit 1)

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r workers/requirements.txt

      - name: Run scraper
        run: |
          set -e
          n=0
          until [ $n -ge 3 ]; do
            python workers/scrape_sbs_en.py && break
            n=$((n+1))
            echo "Scraper failed (attempt $n), retrying in 10s…"
            sleep 10
          done
          if [ $n -ge 3 ]; then
            echo "Scraper failed after retries." >&2
            exit 1
          fi

      - name: Commit changes
        run: |
          git add -A
          if [[ -n "$(git status --porcelain)" ]]; then
            git commit -m "chore: update SBS EN feeds"
          else
            echo "No changes."
          fi

      # ✅ 關鍵 2：改用 -Xtheirs 強制覆蓋，解決 Merge Conflict
      - name: Push with retry
        run: |
          if [[ -n "$(git log origin/main..HEAD)" ]]; then
            n=0
            until [ $n -ge 3 ]; do
              if git push; then
                exit 0
              fi
              echo "Push failed, retrying (attempt $((n+1))/3)…"
              # 這裡的意思是：如果有衝突，優先使用「機械人剛剛生成」的版本
              git pull --rebase -Xtheirs origin main || true
              n=$((n+1))
              sleep 3
            done
            echo "Push still failing after retries." >&2
            exit 1
          else
            echo "Nothing to push."
          fi

      - name: Purge jsDelivr cache
        if: ${{ always() }}
        shell: bash
        run: |
          set -e
          for FILE in "$FEED_JSON" "$FEED_XML"; do
            URL="https://purge.jsdelivr.net/gh/${REPO_SLUG}@main/${FILE}"
            echo "Purging: $URL"
            curl -fsS "$URL" || true
          done

      - name: Warm jsDelivr edge
        if: ${{ always() }}
        shell: bash
        run: |
          set -e
          for FILE in "$FEED_JSON" "$FEED_XML"; do
            URL="https://cdn.jsdelivr.net/gh/${REPO_SLUG}@main/${FILE}"
            for i in 1 2 3; do
              echo "Warming $URL (try $i)…"
              curl -fsS -H "Cache-Control: no-cache" "${URL}?warm=$(date +%s%3N)" > /dev/null || true
              sleep 1
            done
          done
